
{% extends 'base.html.twig' %}

{% block title %}{{pagina|default('Usuario')}}{% endblock %}

{% block body %}
	<div class="card">
        <div class="card-body">
    {{ include('usuarios/_form.html.twig', {'button_label': 'Modificar'}) }}

    
        </div>
    </div>
    
{% endblock %}
