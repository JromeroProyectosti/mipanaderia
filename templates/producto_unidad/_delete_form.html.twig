<form method="post" action="{{ path('producto_unidad_delete', {'id': producto_unidad_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto_unidad_id) }}">
    <button class="btn btn-danger"><span class="fas fa-trash"></span></button>
</form>
