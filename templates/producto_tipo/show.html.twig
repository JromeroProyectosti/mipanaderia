{% extends 'base.html.twig' %}

{% block title %}ProductoTipo{% endblock %}

{% block body %}
    <h1>ProductoTipo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producto_tipo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ producto_tipo.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ producto_tipo.estado ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ producto_tipo.orden }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('producto_tipo_index') }}">back to list</a>

    <a href="{{ path('producto_tipo_edit', {'id': producto_tipo.id}) }}">edit</a>

    {{ include('producto_tipo/_delete_form.html.twig') }}
{% endblock %}
